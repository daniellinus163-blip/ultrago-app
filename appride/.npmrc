; Reduce flaky-install failures on slow or unstable networks (Windows / classroom Wi‑Fi).
fetch-retries=5
; Align with EAS / RN ecosystem when peer ranges are noisy (also used on cloud builders).
legacy-peer-deps=true
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
